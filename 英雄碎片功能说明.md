# 英雄碎片与宠物碎片

## 玩法

- 英雄碎片用于解锁角色与角色皮肤：当前角色 50 片、皮肤 20 片。价格在 `ZRSJZ_Constant.ts` 的 `ZRSJZ_SKIN_CONFIG` 中逐项配置。
- 先解锁角色，再解锁该角色的皮肤。已拥有的角色、皮肤不会重复扣款，旧存档已拥有的内容保留。
- 角色界面新增英雄碎片余额及免费获取入口，参照宠物碎片每天 3 次、每次广告奖励 10 片，两种碎片的领取次数独立。
- 宠物界面的“碎片背光”每秒顺时针旋转 45 度，面板关闭后停止，重新打开继续，不叠加动画。
- 英雄碎片为紫色 1×1 道具，进入各地图原有搜索池；搜索概率沿用紫色物资规则，不保证每个箱子必出。
- 英雄碎片道具及领取界面的图片均位于主包，不依赖 DLC；DLC 未加载也能搜索、显示、结算英雄碎片。
- 盲盒允许抽出宠物碎片和英雄碎片。开箱发奖时直接计入余额，不占仓库，不发仓库溢出邮件。其他道具仍按原规则存仓或发邮件。
- 局内搜到的碎片暂存背包；成功撤离时背包、宠物背包及保险箱中的碎片转余额；撤离失败时只有保险箱碎片保留。双人库存都参与结算，未拾取的箱内物资不入账。

## 存档与同步

- GameData 版本升至 10，新增 `HeroFragments`、`HeroFragmentClaimDate`、`HeroFragmentClaimCount`。
- 从旧版本迁移时先备份为 `ZRSJZ_GameData_before_v10`，补齐新增字段，不清空旧宠物碎片、金币、角色、皮肤或强化进度。
- 碎片奖励通过 `ZRSJZ_FragmentService` 统一处理，保存后发送货币刷新事件。局内碎片删除和余额增加一起保存，重复结算不会重复入账。
- 角色/皮肤购买通过 `ZRSJZ_AccountService.UnlockSkinWithFragments` 校验资格、所有权和余额，扣款与所有权一起保存。

## 验证

- `_data/upgrade/test-fragments.cjs`：旧存档迁移、购买与防重复、每日领取、保存重读、无 DLC 搜索、盲盒发奖、撤离成功/失败/双人保险箱结算。
- `_data/upgrade/check-fragment-types.cjs`：所有本次改动脚本及依赖的类型检查。
- `_data/upgrade/preview-fragments.cjs`：独立浏览器本地预览，验证界面和序列化按钮事件，测试广告回调使用隔离模拟，不连接实际广告平台、不改变真实玩家存档。

## 强化界面补充

- 强化材料区域 `Material0`、`Material1` 作为定位容器，展示统一复用 `TaskAward`。
- 每 5 级为一个材料价值档，每档轮换至少 6 种物资；相邻等级不会使用完全相同的材料组合，后期逐步进入更高价值物资。
- 50 级和全部特殊强化完成后：攻击 +20%、生命 +30%、防御减伤 +20%、大红掉落概率相对 +10%。基础大红概率 10% 时，最终为 11%。
- 所有 `GrayIcon` 的 Sprite SizeMode 为 `TRIMMED`。强化按钮不可点击时隐藏底图，仍保留状态文字。
