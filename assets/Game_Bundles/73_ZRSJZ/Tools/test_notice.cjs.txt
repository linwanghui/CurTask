const fs = require('fs'), vm = require('vm'), assert = require('assert'), path = require('path');
const ts = require('D:/CocosCreator/Creator/3.8.6/resources/app.asar.unpacked/node_modules/typescript/lib/typescript.js');
function load(file, requireMock) {
    const exports = {};
    const output = ts.transpileModule(fs.readFileSync(file, 'utf8'), { compilerOptions: { module: ts.ModuleKind.CommonJS, target: ts.ScriptTarget.ES2019, experimentalDecorators: true } }).outputText;
    vm.runInNewContext(output, { exports, require: requireMock, console });
    return exports;
}
const data = { IsTutorial: false, NoticeClosedDate: '' };
let persisted = '';
const gameData = { Instance: data, SaveData: () => { persisted = JSON.stringify(data); } };
const service = load('Scripts/Service/ZRSJZ_NoticeService.ts', () => ({ ZRSJZ_GameData: gameData })).ZRSJZ_NoticeService;
const today = new Date(2026, 8, 17, 23, 59), tomorrow = new Date(2026, 8, 18, 0, 0);
assert.equal(service.ShouldShow(true, today), false);
data.IsTutorial = true;
assert.equal(service.ShouldShow(false, today), false);
assert.equal(service.ShouldShow(true, today), true);
assert.equal(service.ShouldShow(true, today), true, 'Showing without manual close does not mark read');
service.CloseForToday(today);
assert.equal(service.ShouldShow(true, today), false);
const restarted = load('Scripts/Service/ZRSJZ_NoticeService.ts', () => ({ ZRSJZ_GameData: { Instance: JSON.parse(persisted) } })).ZRSJZ_NoticeService;
assert.equal(restarted.ShouldShow(true, today), false);
assert.equal(restarted.ShouldShow(true, tomorrow), true);
const prefab = JSON.parse(fs.readFileSync('../73_ZRSJZ_DLC/Prefabs/Panel/公告界面.prefab', 'utf8'));
function verify(o) { if (!o || typeof o !== 'object') return; if ('__id__' in o) assert(prefab[o.__id__]); Object.values(o).forEach(verify); }
verify(prefab);
for (const name of ['Mask', 'Tip', 'Panel', '公告底图', '公告', '标题', '版本', '正文滚动区', 'Content', '页码', '上一页', '下一页', '关闭']) assert(prefab.some(o => o._name === name));
const scroll = prefab.find(o => o.__type__ === 'cc.ScrollView');
assert.equal(prefab[scroll._content.__id__]._name, 'Content');
assert.equal(scroll.horizontal, false);
assert.equal(prefab.filter(o => o.__type__ === 'cc.ClickEvent').length, 3);
const constants = load('Scripts/ZRSJZ_Constant.ts', () => ({}));
const defaults = load('Scripts/Service/ZRSJZ_GameDataDefaults.ts', name => name.includes('GameData') ? { ZRSJZ_GameData: { Versions: 13 } } : constants).ZRSJZ_GameDataDefaults;
const old = { Versions: 12, Gold: 3456, PropData: {}, MerchantPurchaseCount: 3 };
const migrated = { ...old }; defaults.Migrate(migrated, old);
assert.equal(migrated.NoticeClosedDate, ''); assert.equal(migrated.Versions, 13);
assert.equal(migrated.Gold, 3456); assert.equal(migrated.MerchantPurchaseCount, 3);
let closed = 0, hidden = 0;
const cc = { _decorator: { ccclass: () => type => type }, Label: 'Label', Button: 'Button', ScrollView: 'ScrollView' };
const panelClass = load('../73_ZRSJZ_DLC/Scripts/Panel/ZRSJZ_NoticePanel.ts', name => {
    if (name === 'cc') return cc;
    if (name.endsWith('/ZRSJZ_Panel')) return { ZRSJZ_Panel: class { Show() {} } };
    if (name.includes('NoticeService')) return { ZRSJZ_NoticeService: { CloseForToday: () => closed++ } };
    if (name.includes('UIManager')) return { ZRSJZ_UIManager: { Instance: { HidePanel: () => hidden++ } } };
    if (name.includes('AudioManager')) return { ZRSJZ_AudioManager: { Instance: { PlaySound() {} } } };
    return constants;
}).ZRSJZ_NoticePanel;
const nodes = {};
for (const name of ['标题', '版本', '页码', '上一页', '下一页']) nodes[name] = { component: {}, getComponent() { return this.component; } };
const body = { string: '', updateRenderData() {} };
nodes['正文滚动区'] = { getComponent: () => ({ content: { getComponent: () => body }, stopAutoScroll() {}, scrollToTop() {} }) };
const panel = new panelClass(); panel.Panel = { getChildByName: name => nodes[name] };
panel.Show(); assert.equal(nodes['上一页'].component.interactable, false);
const click = name => panel.OnButtonClick({ getCurrentTarget: () => ({ name }) });
click('下一页'); assert.equal(nodes['下一页'].component.interactable, false);
click('下一页'); assert.equal(panel.page, 1);
click('上一页'); assert.equal(panel.page, 0);
assert.equal(closed, 0); click('Mask'); assert.equal(closed, 0);
click('关闭'); assert.equal(closed, 1); assert.equal(hidden, 1);
console.log('PASS: tutorial/DLC gates, manual-close persistence, midnight reset, prefab structure, paging boundaries, close handler and v13 migration.');
const config = ts.getParsedCommandLineOfConfigFile(path.resolve('../../../tsconfig.json'), {}, { ...ts.sys, onUnRecoverableConfigFileDiagnostic: console.error });
const program = ts.createProgram(['Scripts/ZRSJZ_Start.ts', '../73_ZRSJZ_DLC/Scripts/Panel/ZRSJZ_NoticePanel.ts'].map(x => path.resolve(x)), { ...config.options, noEmit: true, skipLibCheck: true });
const errors = ts.getPreEmitDiagnostics(program);
const scoped = errors.filter(d => !d.file || /73_ZRSJZ(?:_DLC)?[\\/]/.test(d.file.fileName));
for (const d of scoped) console.error(d.file?.fileName, ts.flattenDiagnosticMessageText(d.messageText, '\n'));
assert.equal(scoped.length, 0);
console.log('TypeScript: main/DLC errors=0, upstream/external=' + (errors.length - scoped.length));
