const fs=require('fs'),path=require('path'),vm=require('vm'),assert=require('assert/strict');
const ts=require('D:/CocosCreator/Creator/3.8.6/resources/app.asar.unpacked/node_modules/typescript/lib/typescript.js');
const root=path.resolve(__dirname,'..');
function load(file,req){const exports={};vm.runInNewContext(ts.transpileModule(fs.readFileSync(path.join(root,file),'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020}}).outputText,{exports,require:req,console,Date,Math});return exports;}
const config=load('Scripts/ZRSJZ_Constant.ts',()=>{throw Error('Unexpected dependency');});
let saved;
const Data={Instance:{PropData:{},PropID:0,HeroFragments:0,PetFragments:0},SaveData(){saved=JSON.stringify(this.Instance);}};
const events={ZRSJZ_EventManager:{EmitPersist(){}},ZRSJZ_MyEvent:{}};
const fragment=load('Scripts/Service/ZRSJZ_FragmentService.ts',id=>id.includes('GameData')?{ZRSJZ_GameData:Data}:events);
const moduleMoon=load('Scripts/Service/ZRSJZ_MidAutumnService.ts',id=>id.includes('GameData')?{ZRSJZ_GameData:Data}:id.includes('FragmentService')?fragment:id.includes('EventManager')?events:config);
const Moon=moduleMoon.ZRSJZ_MidAutumnService;
Moon.Ensure();
assert.equal(Moon.State.offers.length,6);
assert.equal(config.ZRSJZ_PROP_CONFIG.get('炫彩月饼').UnitPrice,1000000);
assert.equal(config.ZRSJZ_PROP_CONFIG.get('炫彩月饼').Quality,config.ZRSJZ_PROP_QUALITY.红色);
assert.equal(Moon.OfferCost('炫彩月饼'),5);
for(let i=0;i<100;i++)assert.equal(Moon.Roll().filter(o=>o.name==='炫彩月饼').length,1);
const boxroom=load('../73_ZRSJZ_DLC/Scripts/ZRSJZ_BoxroomConstant.ts',()=>({}));
assert.equal(boxroom.GetBoxroomCategory('炫彩月饼'),'食用品');
assert.equal(Moon.OfferCost('宠物碎片'),3);
assert.equal(Moon.OfferCost('英雄碎片'),3);
Moon.Pool().forEach(p=>assert.equal(Moon.OfferCost(p.Name),Math.ceil(Math.max(4,Math.ceil(p.UnitPrice/100000))/2)));
const originalOffers=JSON.stringify(Moon.State.offers);
Moon.State.offers.forEach(o=>o.cost=fragment.ZRSJZ_FragmentService.IsFragment(o.name)?5:Math.max(4,Math.ceil(config.ZRSJZ_PROP_CONFIG.get(o.name).UnitPrice/100000)));
Moon.State.offers[0].sold=true;Moon.State.cakes=7;
Moon.Ensure();assert.equal(Moon.State.offers[0].cost,3);assert.equal(Moon.State.offers[0].sold,true);assert.equal(Moon.State.cakes,7);
const repriced=JSON.stringify(Moon.State.offers);Moon.Ensure();assert.equal(JSON.stringify(Moon.State.offers),repriced);
Moon.State.offers=JSON.parse(originalOffers);Moon.State.cakes=0;
console.log('PASS: all offers half price rounded up, existing stock repriced once, balance and sold flags preserved.');
assert.equal(Moon.Pool().every(p=>p.PropType==='物品'&&p.Quality===config.ZRSJZ_PROP_QUALITY.红色),true);
assert.equal(Moon.State.offers.filter(p=>fragment.ZRSJZ_FragmentService.IsFragment(p.name)).length,2);
const offers=JSON.stringify(Moon.State.offers);Moon.Ensure();assert.equal(JSON.stringify(Moon.State.offers),offers);
assert.ok(Moon.Refresh());
const inv=config.ZRSJZ_INVENTORY;
const add=(id,name,where,count=1)=>Data.Instance.PropData[id]={Name:name,CurInventory:where,CurCount:count};
Moon.Materials.forEach((n,i)=>add('bag'+i,n,inv.背包));
assert.ok(Moon.Craft());assert.equal(Moon.State.cakes,0);
Moon.Materials.slice(0,3).forEach((n,i)=>add('store'+i,n,inv.仓库_全部));
const before=JSON.stringify(Data.Instance.PropData);assert.ok(Moon.Craft());assert.equal(JSON.stringify(Data.Instance.PropData),before);
add('oil','油',inv.仓库_物品,2);assert.equal(Moon.Craft(),'');assert.equal(Moon.State.cakes,1);assert.equal(Data.Instance.PropData.oil.CurCount,1);
assert.equal(Object.keys(Data.Instance.PropData).filter(id=>id.startsWith('bag')).length,4);
assert.equal(Moon.Refresh(),'');assert.equal(Moon.State.cakes,0);assert.ok(Moon.Refresh());
assert.equal(Moon.Remaining(),3);
for(let i=0;i<3;i++){const grant=Moon.VideoReward();assert.equal(grant(),true);assert.equal(grant(),false);}
assert.equal(Moon.State.cakes,6);assert.equal(Moon.Remaining(),0);assert.equal(Moon.VideoReward()(),false);
const tomorrow=new Date();tomorrow.setDate(tomorrow.getDate()+1);assert.equal(Moon.Remaining(tomorrow),3);
Moon.State.cakes=100;
assert.equal(Moon.BeginExchange(0),'');assert.equal(Data.Instance.PetFragments,10);
const paid=Moon.State.cakes;assert.ok(Moon.BeginExchange(0));assert.ok(Moon.Refresh());assert.equal(Moon.State.cakes,paid);
const checkpoint=saved;Data.Instance=JSON.parse(checkpoint);assert.equal(Moon.State.pending.name,'宠物碎片');Moon.CompleteExchange();assert.ok(Moon.BeginExchange(0));
assert.equal(Moon.BeginExchange(2),'');const pending=Moon.State.pending;assert.equal(pending.ids.length,1);assert.ok(Data.Instance.PropData[pending.ids[0]]);assert.ok(Moon.BeginExchange(2));Moon.CompleteExchange();
assert.equal(Moon.State.offers[2].sold,true);
console.log('PASS: warehouse-only atomic crafting, red-only pool plus fragments, saved offers, refresh cost, daily 3x2 and duplicate callbacks, exchange/recovery.');
const prefab=JSON.parse(fs.readFileSync(path.resolve(root,'../73_ZRSJZ_DLC/Prefabs/Panel/活动界面.prefab'),'utf8'));
function at(p){let n=prefab[1];for(const name of p.split('/')){n=n._children.map(r=>prefab[r.__id__]).find(v=>v._name===name);assert.ok(n,p);}return n;}
for(const p of ['Panel/活动选项/月满金秋','Panel/中秋背景','Panel/右栏/月满金秋/兑换格模板/兑换','Panel/右栏/月满金秋/兑换格模板/详情','Panel/右栏/月满金秋/刷新','Panel/右栏/月满金秋/制作','Panel/右栏/月满金秋/广告领取'])at(p);
Moon.Materials.forEach(name=>at('Panel/右栏/月满金秋/材料栏/'+name+'/数量'));
const ids=new Set();prefab.forEach((v,i)=>{if(v.fileId){assert.ok(!ids.has(v.fileId));ids.add(v.fileId);}if(v.__type__==='cc.Node'){v._children.forEach(r=>assert.equal(prefab[r.__id__]._parent.__id__,i));v._components.forEach(r=>assert.equal(prefab[r.__id__].node.__id__,i));}});
console.log('PASS: activity prefab paths, ownership, unique serialized IDs.');
Data.Versions=24;
const defaults=load('Scripts/Service/ZRSJZ_GameDataDefaults.ts',id=>id.includes('MidAutumnService')?moduleMoon:id.includes('GameData')?{ZRSJZ_GameData:Data}:id.includes('Constant')?config:{}).ZRSJZ_GameDataDefaults;
const legacy={Versions:23,Gold:1234567,PropData:{},InventoryRow:{仓库_全部:25},ActivityExtractionValue:9000000,ActivityTaskClaimed:[0,1],ActivityRewardClaimed:[0],OwnedTitles:['勇者'],EquippedTitle:'勇者',OwnedAvatarFrames:['1'],CurrentAvatarFrame:'1'};
defaults.Migrate(legacy,JSON.parse(JSON.stringify(legacy)));
assert.equal(legacy.Versions,24);assert.equal(legacy.MidAutumn.cakes,0);assert.equal(legacy.Gold,1234567);assert.equal(legacy.InventoryRow.仓库_全部,25);assert.equal(legacy.ActivityExtractionValue,9000000);assert.deepEqual(legacy.ActivityTaskClaimed,[0,1]);
legacy.MidAutumn.cakes=7;defaults.Migrate(legacy,JSON.parse(JSON.stringify(legacy)));assert.equal(legacy.MidAutumn.cakes,7);
console.log('PASS: v23->v24 preserves currency, warehouses and sprint progress; repeated migration preserves mooncake balance.');
Data.Instance.PropData={};
assert.equal(Moon.CanCraft(),false);
Moon.Materials.forEach((name,i)=>add('onlyBag'+i,name,inv.背包));
assert.equal(Moon.CanCraft(),false);
Moon.Materials.forEach((name,i)=>add('reminder'+i,name,inv.仓库_物品));
assert.equal(Moon.CanCraft(),true);
let sprint=false;
const reminders=load('Scripts/Service/ZRSJZ_MainReminderService.ts',id=>id.includes('MidAutumn')?moduleMoon:id.includes('ActivityService')?{ZRSJZ_ActivityService:{HasClaimable:()=>sprint}}:id.includes('Achievement')?{ZRSJZ_AchievementService:{HasClaimableRewards:()=>false}}:fragment).ZRSJZ_MainReminderService;
assert.equal(reminders.GetReminders(true).活动,true);
assert.equal(reminders.GetReminders(false).活动,false);
assert.equal(Moon.Craft(),'');assert.equal(Moon.CanCraft(),false);
assert.equal(reminders.GetReminders(true).活动,false);
sprint=true;assert.equal(reminders.GetReminders(true).活动,true);
at('Panel/活动选项/月满金秋/红点');at('Panel/右栏/月满金秋/制作/红点');
console.log('PASS: crafting reminders follow warehouse ingredients, hide after crafting, obey DLC gate and preserve sprint reminders.');
