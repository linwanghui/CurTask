const fs=require('fs'),vm=require('vm'),path=require('path'),assert=require('assert');
const ts=require('D:/CocosCreator/Creator/3.8.6/resources/app.asar.unpacked/node_modules/typescript/lib/typescript.js');
const Online={Battle:true,BattleHost:false,BattleEnded:true,CombatStates:new Map(),Disconnect(){assert.equal(this.Battle,false);this.disconnected=true;}};
const Coop={};let tips=0,applied=0,restored=0,lastTip='';
const Enemy={OnlineEnemies:new Map()};
const game={GamePaused:false,IsGameFinished:false,CurMap:{Unit:{isValid:true,getComponentsInChildren:()=>[{RestoreSuppressedEnemy(){restored++;}}]}}};
const deps={ZRSJZ_OnlineService:Online,ZRSJZ_OnlineCombat:Coop,ZRSJZ_EnemyBase:Enemy,ZRSJZ_Game:{Instance:game},ZRSJZ_UIManager:{Instance:{ShowTip(message){tips++;lastTip=message;}}}};
const source=fs.readFileSync(path.resolve(__dirname,'../Scripts/Controller/ZRSJZ_OnlineBattle.ts'),'utf8');
const code=ts.transpileModule(source,{compilerOptions:{module:1,target:7,experimentalDecorators:true}}).outputText;
const out={};vm.runInNewContext(code,{exports:out,console,require:n=>n==='cc'?{_decorator:{ccclass:()=>c=>c},Component:class{},Vec3:class{}}:deps});
const Battle=out.ZRSJZ_OnlineBattle;
const state={id:'e1',hp:37,dead:false};Online.CombatStates.set('e1',state);
Enemy.OnlineEnemies.set('e1',{isValid:true,TakeOverLocally(s){assert.equal(s.hp,37);applied++;}});
const b=new Battle();b.OnBattleEnd();assert.equal(game.GamePaused,false);assert.equal(applied,1);assert.equal(Online.Battle,false);assert.equal(tips,1);
b.OnBattleEnd();b.OnPeerLeft();b.ContinueSolo();assert.equal(applied,1);assert.equal(tips,1);
Online.Battle=true;Online.BattleHost=false;Online.CombatStates.clear();game.GamePaused=true;
const opening=new Battle();opening.OnBattleEnd();assert.equal(game.GamePaused,true);assert.equal(restored,1);
Online.Battle=true;Online.BattleHost=true;game.IsGameFinished=true;const before=tips;
new Battle().OnBattleEnd();assert.equal(game.IsGameFinished,true);assert.equal(game.GamePaused,true);assert.equal(tips,before);
console.log('PASS: battle_end/peer_left -> solo, idempotent, no forced pause/reset, loading fallback, finished host unchanged.');
game.IsGameFinished=false;Online.Battle=true;Online.BattleHost=false;
const evac=new Battle();evac.OnBattleEnd('evacuated');assert(lastTip.startsWith('队友已撤离'));assert(!lastTip.includes('连接中断'));
evac.OnPeerLeft();assert(lastTip.startsWith('队友已撤离'));
console.log('PASS: evacuation tip preserved after subsequent disconnect notification.');
// Voluntary escape from teammate pause must use the same takeover, not settle/reload the map.
const sent=[];Online.Send=type=>sent.push(type);Online.LocalHolds=new Set();Online.PeerWaiting=true;
Online.Battle=true;Online.BattleHost=false;Online.CombatStates.clear();game.GamePaused=true;
const waitingGuest=new Battle();waitingGuest.LeaveWaitingRoom();assert.equal(Online.Battle,false);assert.equal(game.GamePaused,false);assert.equal(sent.at(-1),'leave');assert(lastTip.startsWith('已离开房间'));
const tipCount=tips;waitingGuest.LeaveWaitingRoom();assert.equal(tips,tipCount);
Online.Battle=true;Online.BattleHost=true;game.GamePaused=true;
const waitingHost=new Battle();let snapshots=0;waitingHost.SyncEnemies=()=>snapshots++;
waitingHost.LeaveWaitingRoom();assert.equal(snapshots,1);assert.equal(Online.Battle,false);assert.equal(game.GamePaused,false);
Online.Battle=true;Online.BattleHost=false;Online.LocalHolds.add('ad');
const blocked=new Battle();blocked.LeaveWaitingRoom();assert.equal(Online.Battle,true);Online.LocalHolds.clear();
game.CurPlayer={IsDead:true};game.GamePaused=true;blocked.LeaveWaitingRoom();assert.equal(Online.Battle,false);assert.equal(game.GamePaused,true);
console.log('PASS: guest/host leave-wait continues solo, sends leave, no repeat action, local ad guarded, death pause preserved.');
