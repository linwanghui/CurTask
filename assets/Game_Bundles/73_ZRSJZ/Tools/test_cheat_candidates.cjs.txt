const fs=require('fs'),vm=require('vm'),path=require('path'),assert=require('assert');
const ts=require('D:/CocosCreator/Creator/3.8.6/resources/app.asar.unpacked/node_modules/typescript/lib/typescript.js');
let saves=0,tips=[];const data={Instance:{BossExtractionCompleted:{unrelated:true}},SaveData(){saves++;}};
const deps={ZRSJZ_GameData:data,ZRSJZ_Panel:class{},ZRSJZ_PROP_CONFIG:new Map(['坦克模型','克劳模型','飞机','坦克'].map(n=>[n,{}])),ZRSJZ_UIManager:{Dragging:false,Instance:{ShowTip:t=>tips.push(t)}}};
function load(file){const exports={};vm.runInNewContext(ts.transpileModule(fs.readFileSync(path.resolve(__dirname,'../'+file),'utf8'),{compilerOptions:{module:1,target:7,experimentalDecorators:true}}).outputText,{exports,console,require:n=>n==='cc'?{_decorator:{ccclass:()=>c=>c,property:()=>()=>{}}}:deps});return exports;}
const Service=load('Scripts/Service/ZRSJZ_LevelProgressService.ts').ZRSJZ_LevelProgressService;
Service.UnlockAll();assert.equal(saves,1);assert(data.Instance.BossExtractionCompleted.unrelated);
for(const map of ['五号小镇','沙漠古迹','极北之地'])for(const mode of ['机密行动','绝密行动'])assert.equal(Service.GetLockReason(map+'_'+mode),'');
const Panel=load('Scripts/Panel/ZRSJZ_CheatingPanel.ts').ZRSJZ_CheatingPanel;
assert.deepEqual(Array.from(Panel.FindRelatedProps('坦克')),['坦克模型','克劳模型','坦克']);
assert.deepEqual(Array.from(Panel.FindRelatedProps('坦')),['坦克模型','坦克']);
assert.equal(Panel.FindRelatedProps('  ').length,0);assert.equal(Panel.FindRelatedProps('不存在').length,0);
const p=new Panel();p.PropName={string:' 坦克 '};p.PropCount={string:'3'};let added=null,rendered=0;
p.AddSelected=(n,c)=>added=[n,c];p.RenderMatches=()=>rendered++;
const click=()=>p.OnButtonClick({getCurrentTarget:()=>({name:'添加道具'})});
click();assert.deepEqual(added,['坦克',3]);assert.equal(rendered,0);
p.PropName.string='模型克';click();assert.equal(rendered,1);assert.equal(p._matchCount,3);assert.equal(p._matches.length,3);
p.PropCount.string='2abc';click();assert.equal(rendered,1);assert(tips.at(-1).includes('正整数'));
p.PropCount.string='0';click();assert(tips.at(-1).includes('正整数'));
p.PropCount.string='2';p.PropName.string='  ';click();assert(tips.at(-1).includes('关键字'));
console.log('PASS: all six levels unlocked/saved without clearing other records; per-character OR matching; exact match direct add; quantity preserved; invalid/empty input rejected.');
