const fs = require('fs');
const vm = require('vm');
const assert = require('assert');
const path = require('path');
const ts = require('D:/CocosCreator/Creator/3.8.6/resources/app.asar.unpacked/node_modules/typescript/lib/typescript.js');
const root = path.resolve(__dirname, '..');
const data = { ActivityExtractionValue: 0, ActivityTaskClaimed: [], ActivityRewardClaimed: [] };
const fakeData = { Instance: data, SaveData() {} };
const js = ts.transpileModule(fs.readFileSync(path.join(root, 'Scripts/Service/ZRSJZ_ActivityService.ts'), 'utf8'), { compilerOptions: { module: ts.ModuleKind.CommonJS, target: ts.ScriptTarget.ES2020 } }).outputText;
const mod = { exports: {} };
vm.runInNewContext(js, { exports: mod.exports, require: () => ({ ZRSJZ_GameData: fakeData }) });
const service = mod.exports.ZRSJZ_ActivityService;
(async () => {
    assert.equal(service.HasClaimable(), false);
    [-1, 0, NaN, Infinity].forEach(value => service.RecordExtraction(value));
    assert.equal(service.Value, 0);
    service.RecordExtraction(999999);
    assert.equal(service.CanClaimTask(0), false);
    service.RecordExtraction(1);
    assert.equal(service.CanClaimTask(0), true);
    assert.equal(service.Points, 0);
    assert.equal(service.ClaimTask(0), true);
    assert.equal(service.ClaimTask(0), false);
    assert.equal(service.Points, 10);
    service.RecordExtraction(99000000);
    service.Tasks.forEach((_, i) => service.ClaimTask(i));
    assert.equal(service.Points, 200);
    let grantCount = 0;
    const results = await Promise.all([service.ClaimReward(0, async () => { grantCount++; }), service.ClaimReward(0, async () => { grantCount++; })]);
    assert.equal(grantCount, 1);
    assert.equal(results.filter(Boolean).length, 1);
    await assert.rejects(() => service.ClaimReward(1, async () => { throw Error('test'); }));
    assert.equal(service.RewardClaimed(1), false);
    for (let i = 1; i < service.Rewards.length; i++) assert.equal(await service.ClaimReward(i, async () => {}), true);
    assert.equal(service.HasClaimable(), false);
    assert.equal(service.Points, 200);
    console.log('PASS: extraction boundaries, invalid values, manual points, duplicate claims, async lock, retry, all rewards/red dots.');
    const configPath = path.resolve(root, '../../../tsconfig.json');
    const config = ts.readConfigFile(configPath, ts.sys.readFile);
    const parsed = ts.parseJsonConfigFileContent(config.config, ts.sys, path.dirname(configPath));
    const program = ts.createProgram(parsed.fileNames.filter(file => file.endsWith('.ts')), { ...parsed.options, noEmit: true });
    const diagnostics = ts.getPreEmitDiagnostics(program);
    const scoped = diagnostics.filter(d => d.file && /Game_Bundles[\\/]73_ZRSJZ(?:_DLC)?[\\/]/.test(d.file.fileName));
    console.log('TypeScript project diagnostics:', diagnostics.length, 'main/DLC:', scoped.length);
    for (const d of scoped) console.log(d.file.fileName, ts.flattenDiagnosticMessageText(d.messageText, '\n'));
    if (scoped.length) process.exitCode = 1;
})().catch(error => { console.error(error); process.exitCode = 1; });
